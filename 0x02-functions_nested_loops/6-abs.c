#include "main.h"
/**
 * _abs - Function that computes the absolute value
 * of an integer
 * Return: Absolue value
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1)
	}
}
